{
  "name": "idrive-lao-pro",
  "private": true,
  "version": "8.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5180 --strictPort --host",
    "live": "node scripts/live.mjs",
    "server": "node server/index.mjs",
    "build": "vite build",
    "preview": "vite preview --port 4180 --strictPort --host",
    "start": "node server/index.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
